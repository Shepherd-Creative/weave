# @shepherd-creative/weave-docs

Docs site for Weave. **Scaffolded in B3, populated in B6** (OSS release).

Will be a Next.js 15 app with:
- Landing page introducing the primitives + MCP model
- Per-primitive pages with live spec playground
- SKILL.md rendered as guided documentation
- Migration + integration guides

Not buildable yet — the `build` / `dev` scripts are no-ops during B3.
