node_modules
dist
.turbo
.next
.DS_Store
*.tsbuildinfo
coverage
.env
.env.local
.env.*.local
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claude Code local overrides
CLAUDE.local.md
.claude/settings.local.json

# Session-concurrency worktrees (each checkout carries its own biome.json;
# committed ignore needed because tools like biome don't read .git/info/exclude)
.claude/worktrees/
